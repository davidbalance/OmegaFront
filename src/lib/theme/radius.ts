import { MantineRadius, MantineRadiusValues, rem } from "@mantine/core";

export const defaultRadius: MantineRadius = 'sm'
export const radiusValues: MantineRadiusValues = {
    xs: rem(2),
    sm: rem(4),
    md: rem(8),
    lg: rem(12),
    xl: rem(16)
}