import { rem, Skeleton } from '@mantine/core'
import React from 'react'

const OmegaHeaderMenuSuspense: React.FC = () => {
    return (
        <Skeleton circle h={rem(30)} />
    )
}

export default OmegaHeaderMenuSuspense